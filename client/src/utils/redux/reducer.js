
const initialState = {
    none: []
}

const reducer = (state=initialState, action) => {
    switch (action.type) {
        case "value":
            
            break;
    
        default:
            break;
    }
}

export default reducer;